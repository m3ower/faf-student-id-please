# Add these lines to the CPR's .env.example (with placeholder values)
# and to your own .env (with real values). Never commit .env.
DOCKERHUB_USER=m3ower
MODERATION_VERSION=0.1.0
MODERATION_DB_NAME=moderation
MODERATION_DB_USER=moderation
MODERATION_DB_PASSWORD=change-me
DMS_VERSION=0.1.0
DMS_REDIS_PASSWORD=change-me
